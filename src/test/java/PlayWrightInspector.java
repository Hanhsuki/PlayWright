public class PlayWrightInspector {
    //PWDEBUG: setting laf 0, 1 or console
    //PLAYWRIGHT_JAVA_SCR : bằng đường dẫn đến file main/java của project và đường dẫn đến test/java của project
    //C2: sử dụng hàm pause (Example: siginPage.pause(). sẽ dừng lại tại điểm đặt pause và hiển thị playWright Inspector
    //C3: Debug selector. nếu set PWDEBUG = console, chúng ta có thể dùng debug point của IDE để debug
    //Khi run đến điểm debug point. f12 vào tab console : playwright.$("element cần debug") => sẽ hiển thị html của element
    //playwright.inspect(); playwright.locator();playwright.highlight(); playwright.clear(); ......

}
